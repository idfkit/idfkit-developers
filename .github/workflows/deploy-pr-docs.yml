name: Deploy PR Docs Preview

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: pr-docs-preview-${{ github.event.number }}
  cancel-in-progress: true

jobs:
  deploy-preview:
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Check out gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages-deploy

      - name: Set up the environment
        uses: ./.github/actions/setup-python-env

      - name: Set up the parity ledger
        uses: ./.github/actions/setup-parity-ledger

      - name: Install Cairo
        run: |
          sudo apt-get update
          sudo apt-get install -y libcairo2

      - name: Build documentation
        # The preview is published to pr-preview/pr-N/ on this repository's gh-pages branch, and
        # that branch is what developers.idfkit.com serves (deploy-docs.yml, FR-051). So the
        # preview's address is on the unified host, and site_url has to say so: every canonical
        # link, sitemap entry and social card in the preview build is derived from it.
        #
        # py.idfkit.com is not that address any more. It is served from idfkit/py.idfkit.com and
        # carries redirect-only content (deploy-py-redirects.yml, FR-056), so a preview link there
        # would land on the unified site root instead of on the preview.
        run: |
          PREVIEW_URL="https://developers.idfkit.com/pr-preview/pr-${{ github.event.number }}/"
          sed -i "s|^site_url:.*|site_url: ${PREVIEW_URL}|" mkdocs.yml
          ./scripts/build_docs.sh

      - name: Fetch base branch for diff
        run: git fetch origin ${{ github.event.pull_request.base.ref }} --depth=1

      - name: Find changed doc pages
        id: diff-docs
        run: |
          base="origin/${{ github.event.pull_request.base.ref }}"
          pages=""

          # Markdown files map directly to pages
          md_pages=$(git diff --name-only "$base" -- docs/ \
            | grep '\.md$' \
            | sed 's|^docs/||; s|/index\.md$|/|; s|\.md$|/|')
          pages="${pages}${md_pages}"$'\n'

          # Snippets/notebooks: find which .md files include each changed non-md file
          non_md=$(git diff --name-only "$base" -- docs/snippets/ docs/*.ipynb || true)
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            # Search for markdown files that reference this snippet path
            includers=$(grep -Frl "$f" docs/ --include='*.md' 2>/dev/null \
              | sed 's|^docs/||; s|/index\.md$|/|; s|\.md$|/|' || true)
            pages="${pages}${includers}"$'\n'
          done <<< "$non_md"

          pages=$(echo "$pages" | grep -v '^$' | sort -u)
          {
            echo "pages<<EOF"
            echo "$pages"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Deploy preview to gh-pages
        run: |
          rm -rf "gh-pages-deploy/pr-preview/pr-${{ github.event.number }}"
          mkdir -p "gh-pages-deploy/pr-preview/pr-${{ github.event.number }}"
          cp -r site/. "gh-pages-deploy/pr-preview/pr-${{ github.event.number }}/"

          cd gh-pages-deploy
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || {
            git commit -m "Deploy docs preview for PR #${{ github.event.number }}"
            git pull --rebase origin gh-pages
            git push origin gh-pages
          }

      - name: Add or update preview comment
        uses: actions/github-script@v7
        env:
          CHANGED_PAGES: ${{ steps.diff-docs.outputs.pages }}
        with:
          script: |
            // The same address the build above was given as site_url, and the same one the
            // deploy step below writes to. All three name gh-pages/pr-preview/pr-N/ on
            // developers.idfkit.com.
            const previewUrl = `https://developers.idfkit.com/pr-preview/pr-${context.issue.number}/`;
            const marker = '<!-- docs-preview-comment -->';

            const changedPages = (process.env.CHANGED_PAGES || '').trim();
            let changedSection = '';
            if (changedPages) {
              const links = changedPages.split('\n').map(p => `- ${previewUrl}${p}`).join('\n');
              changedSection = `\n\n**Changed pages:**\n${links}`;
            }

            const body = [
              marker,
              `**Docs preview** for this PR is available at:`,
              previewUrl,
              changedSection,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find(c => c.body.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  cleanup-preview:
    if: github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Check out gh-pages branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Remove preview directory
        run: |
          if [ -d "pr-preview/pr-${{ github.event.number }}" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git rm -rf "pr-preview/pr-${{ github.event.number }}"
            git commit -m "Remove docs preview for PR #${{ github.event.number }}"
            git pull --rebase origin gh-pages
            git push
          fi
